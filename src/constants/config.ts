const config = {
  defaultPath: '/dashboard/default',
  fontFamily: `'Public Sans', sans-serif`,
  i18n: 'en',
  miniDrawer: false,
  container: true,
  mode: 'light',
  presetColor: 'default',
  themeDirection: 'ltr',
  home : 'packing',
  theme: 'TopBar'
};

export default config;

export const drawerWidth : number = 220;
export const drawerMiniWidth : number = 79;
export const headerHight : number = 55;